[package]
name = "backtester-py"
version = "0.1.1"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
backtester-core = { path = "../backtester-core" }
pyo3 = { version = "0.22", features = ["extension-module", "abi3-py39"] }
arrow = { version = "53", features = ["pyarrow"] }
rayon = "1.11.0"
