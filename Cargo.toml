[workspace]
members = [
    "backtester-core",
    "backtester-py",
]
default-members = [
    "backtester-core",
]
resolver = "2"


[profile.release]
lto = "fat"
codegen-units = 1
opt-level = 3
panic = "abort"
strip = true
